# modules/redis/.env.example — copied to .env by lib/env.sh (git-ignored).
#
# The password uses a __GEN__ placeholder: generated on first run, shown once,
# never rotated on re-run.

# Strong random password (auto-generated). Clients must AUTH with this.
REDIS_PASSWORD=__GEN:alnum:40__
